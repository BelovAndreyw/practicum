# Minimal nginx for Let's Encrypt http-01 challenge (bootstrap only)
server {
    listen 80;
    server_name teamzachet.ru www.teamzachet.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 404;
    }
}

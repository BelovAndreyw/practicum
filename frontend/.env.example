# Backend URL used by Vite dev proxy (/api -> VITE_BACKEND_URL)
VITE_BACKEND_URL=http://localhost:8080

# Common API prefix
VITE_API_BASE=/api

# IMPORTANT: keep false for backend integration and deployment
VITE_USE_MOCK=false
